<h1>Activity</h1>

<%= form_for @event, url: events_create_path(@event) do |f| %>
  <%= f.label :message %>
  <%= f.text_area :message %>
  <%= f.submit %>
<% end %>

<div id="events">
  <%= render @events %>
</div>
